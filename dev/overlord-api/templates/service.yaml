apiVersion: v1
kind: Service
metadata:
  name: {{ include "overlord-api.fullname" . }}
  labels:
    {{- include "overlord-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "overlord-api.selectorLabels" . | nindent 4 }}
