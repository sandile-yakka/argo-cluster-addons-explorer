apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "overlord-api.fullname" . }}-test-connection"
  labels:
    {{- include "overlord-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "overlord-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
